<%- include('includes/head.ejs') %>
</head>
<body>
    <%- include('includes/navigation.ejs') %>

    <main>
        <% if (prods.length > 0) { %>
            <div class="grid">
                <% for (let producto of prods) { %>
                    <article class="card product-item">
                        <header class="card__header">
                            <h1 class="product__title"><%= producto.nombre %></h1>
                        </header>
                        <div class="card__image">
                            <img src="https://cdn.pixabay.com/photo/2017/03/20/22/13/book-2160539_640.png"
                                alt="Un Libro">
                        </div>
                        <div class="card__content">
                            <h2 class="product__price">S/ 19.99</h2>
                            <p class="product__description">Un libro sobre la historia de America</p>
                        </div>
                        <div class="card__actions">
                            <button class="btn">Agregar al carrito</button>
                        </div>
                    </article>
                <% } %>
            </div>
        <% } else { %>
            <h1>No hay productos</h1>
        <% } %>
    </main>
    
<%- include('includes/end.ejs') %>